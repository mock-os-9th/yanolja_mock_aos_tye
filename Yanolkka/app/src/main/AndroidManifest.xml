<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yanolkka">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".src.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".src.activities.room_details.RoomDetailsActivity"></activity>
        <activity android:name=".src.activities.search_region_result.SearchRegionResultActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".src.activities.maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".src.activities.profile.ProfileActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".src.activities.settings.SettingsActivity" />
        <activity android:name=".src.activities.num_people.NumPeopleActivity" />
        <activity android:name=".src.activities.motel.SearchMotelActivity" />
        <activity android:name=".src.activities.calendar.CalendarActivity" />
        <activity android:name=".src.common.yet.YetActivity" />
        <activity
            android:name=".src.activities.sign_up.SignUpActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".src.activities.reservation.ReservationActivity" >
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity
            android:name=".src.activities.room_info.RoomInfoActivity"
            android:theme="@style/TranslucentStatusBar" />
        <activity android:name=".src.activities.search_result.SearchResultActivity" />
        <activity android:name=".src.activities.search.SearchActivity" />
        <activity android:name=".src.activities.sign_in.SignInActivity" />
        <activity android:name=".src.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
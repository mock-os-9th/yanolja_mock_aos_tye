<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yanolkka">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".src.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".src.activities.num_people.NumPeopleActivity"></activity>
        <activity android:name=".src.activities.motel.SearchMotelActivity" />
        <activity android:name=".src.activities.calendar.CalendarActivity" />
        <activity android:name=".src.common.activities.YetActivity" />
        <activity
            android:name=".src.activities.sign_up.SignUpActivity"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity android:name=".src.activities.reservation.ReservationActivity" />
        <activity android:name=".src.activities.room_info.RoomInfoActivity" />
        <activity android:name=".src.activities.search_result.SearchResultActivity" />
        <activity android:name=".src.activities.search.SearchActivity" />
        <activity android:name=".src.activities.sign_in.SignInActivity" />
        <activity android:name=".src.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>